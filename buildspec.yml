version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install -r requirements.txt --target ./package

  build:
    commands:
      - echo "Running tests..."
      - python -m pytest tests/ -v || true

      - echo "Packaging application..."

      # Clean and prepare deploy folder
      - rm -rf deploy && mkdir deploy


      # Create app directory in deploy and copy app source files (not the folder itself)
      - mkdir -p deploy/app
      - cp app/*.py deploy/app/
      - cp requirements.txt Procfile pyproject.toml README.md deploy/ || true

      # Zip the CONTENTS of deploy/ (not the folder itself)
      - cd deploy
      - zip -r ../app.zip .
      - cd ..

artifacts:
  files:
    - app.zip
  discard-paths: yes